[color]
    ui = auto
[core]
    pager = less -FMRiX
    editor = /usr/bin/vim
[push]
    default = simple
[alias]
    dag = log --graph --format='format:%C(yellow)%h%C(reset) %C(blue)\"%an\" <%ae>%C(reset) %C(magenta)%cr%C(reset)%C(auto)%d%C(reset)%n%s' --date-order
[commit]
    template = /Users/milkin/template.txt
